import { describe, it, expect } from 'vitest'
import { createApp } from 'vue'
import EditFormVue from '../list/EditForm.vue'
import AppVue from '@/App.vue'

describe('HomeView', () => {
  it('할 일을 생성합니다.', () => {})

  it('할 일을 수정합니다.', () => {})

  it('Category를 선택 후 통계 페이지로 이동 후 다시 목록 페이지로 이동했을 때 Category가 유지됩니다.', () => {})

  it('검색어 입력시 Category에 해당되는 값 중 검색어를 포함한 할 일 아이템만 목록에 노출됩니다.', () => {})
})
